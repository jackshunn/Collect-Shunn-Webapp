{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Node Functions",
            "type": "node",
            "request": "attach",
            "port": 9229,
            "envFile": "${workspaceRoot}/.env",
            "preLaunchTask": "func: host start"
        }
    ]
}